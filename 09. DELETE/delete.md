# Excluindo dados com DELETE

O comando `DELETE` é essencialmente utilizado para excluir valores indesejados da sua base de dados. Com isso, para remover o valor inserido anteriormente no último comando, basta escrever:

```sql
DELETE FROM Artist WHERE Name = 'Banda Exemplo';
```